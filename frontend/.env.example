# API Django (sans slash final). Optionnel en dev : sans cette ligne, le frontend
# utilise /api/v1 et le proxy Vite (vite.config.ts) vers http://127.0.0.1:8000.
# VITE_API_URL=http://127.0.0.1:8000/api/v1
