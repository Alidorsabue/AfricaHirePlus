# Évite l’avertissement npm « Use --omit=dev instead » quand NODE_ENV=production pendant l’install
# (Vite / TypeScript sont en devDependencies — ils doivent être installés au build).
production=false
